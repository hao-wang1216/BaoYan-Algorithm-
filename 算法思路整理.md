# 算法思路整理

本仓库当前算法题主要集中在根目录，`AcWing/` 和 `LeetCode/` 子目录暂未放入题解文件。下面按知识点整理每道题的核心思路、常用模板和注意点。

## 一、基础算法

### 786. 第 k 个数

- 算法：快速选择。
- 思路：沿用快排的分治划分思想，每次选一个枢轴，把数组分成小于等于/大于等于两部分，只递归包含第 k 小的那一边。
- 关键：
  - 设左半段长度 `sl = j - l + 1`。
  - 若 `k <= sl`，答案在左半段；否则在右半段找第 `k - sl` 小。
- 复杂度：平均 `O(n)`，最坏 `O(n^2)`。

### 802. 区间和

- 算法：离散化 + 前缀和。
- 思路：坐标范围很大但实际用到的点少，把所有加点位置和查询端点收集到 `alls`，排序去重后映射成连续下标。
- 关键：
  - `find(x)` 用二分找离散化下标。
  - 对离散化后的数组做单点加，再做前缀和。
  - 查询 `[l, r]` 时返回 `s[r] - s[l - 1]`。
- 复杂度：排序 `O((n + m)log(n + m))`，每次映射 `O(log(n + m))`。

### 803. 区间合并

- 算法：排序 + 扫描。
- 思路：先按左端点排序，维护当前合并区间 `[st, ed]`。若新区间左端点大于 `ed`，说明不相交，保存旧区间并开启新区间；否则更新右端点。
- 关键：
  - 判断相交：`seg.first <= ed`。
  - 更新右端点：`ed = max(ed, seg.second)`。
- 注意：当前代码末尾 `if(st=-2e9)` 是赋值，不是判断，应为 `if(st != -2e9)`，否则结果会错。

## 二、栈、队列、链表与堆

### 154. 滑动窗口

- 算法：单调队列。
- 思路：队列里存数组下标，并维护窗口合法性和单调性。
- 求最小值：
  - 队头若滑出窗口就弹出。
  - 队尾元素若大于当前元素就弹出，保持队列单调递增。
  - 队头就是当前窗口最小值。
- 求最大值：
  - 同理维护单调递减队列。
- 复杂度：每个元素最多入队出队一次，`O(n)`。
- 注意：当前最大值循环里 `while(hh<=tt&a[q[tt]]<a[i])` 使用了单个 `&`，建议改成逻辑与 `&&`。

### 826. 单链表

- 算法：数组模拟单链表。
- 思路：用 `e[i]` 存节点值，`ne[i]` 存下一个节点下标，`idx` 表示下一个可用位置。
- 操作：
  - 头插：新节点指向原 `head`，再更新 `head`。
  - 在第 k 个插入后插入：修改新节点和第 k 个节点的 `next`。
  - 删除第 k 个后面的点：`ne[k] = ne[ne[k]]`。
- 注意：删除头节点时应单独处理，避免继续调用 `remove(k - 1)` 导致越界或逻辑错误。

### 827. 双链表

- 状态：当前文件为空。
- 常用思路：数组模拟双链表，用 `l[i]`、`r[i]` 表示左右指针，通常设置 0 为左端哨兵，1 为右端哨兵。
- 核心操作：
  - 在节点 `k` 右侧插入 `x`：更新新节点、右邻节点、`k` 三方指针。
  - 删除节点 `k`：`r[l[k]] = r[k]`，`l[r[k]] = l[k]`。

### 830. 单调栈

- 算法：单调栈。
- 思路：对每个数 `x`，弹出所有大于等于 `x` 的栈顶元素，剩下的栈顶就是左边第一个比 `x` 小的数。
- 关键：栈中保持严格递增。
- 复杂度：每个元素最多入栈出栈一次，`O(n)`。

### 838. 堆排序

- 状态：当前文件为空。
- 常用思路：用数组维护小根堆。
- 核心：
  - `down(u)` 把节点 `u` 下沉到合法位置。
  - 建堆从 `n / 2` 到 `1` 逆序 `down`。
  - 每次输出堆顶，再用堆尾覆盖堆顶并 `down(1)`。
- 复杂度：建堆 `O(n)`，取前 m 小 `O(m log n)`。

## 三、并查集与哈希结构

### 836. 合并集合

- 算法：并查集。
- 思路：每个集合用根节点代表，`find(x)` 找根并路径压缩。
- 操作：
  - 合并：`p[find(a)] = find(b)`。
  - 查询：判断 `find(a) == find(b)`。
- 复杂度：近似 `O(1)`。

### 837. 连通块中点的数量

- 算法：带集合大小的并查集。
- 思路：在普通并查集基础上维护 `s[root]`，表示当前连通块点数。
- 关键：
  - 合并前先判断两个点是否已在同一集合。
  - 合并时把一个根挂到另一个根，并把大小累加到新根上。
- 查询：
  - `Q1 a b`：是否同块。
  - `Q2 a`：输出 `s[find(a)]`。

### 840. 模拟散列表

- 算法：开放寻址哈希。
- 思路：数组 `h[]` 存值，空位用特殊值 `null` 标记。
- 关键：
  - 哈希地址：`k = (x % N + N) % N`，兼容负数。
  - 冲突时向后线性探测。
  - `find(x)` 返回 `x` 已存在的位置，或第一个空位。
- 复杂度：均摊 `O(1)`。

## 四、字符串算法

### 835. Trie 字符串

- 算法：Trie 字典树。
- 思路：每个节点有 26 条边，插入字符串时沿字符路径向下走，不存在就新建节点。
- 关键：
  - `son[p][u]` 表示节点 `p` 通过字符 `u` 到达的子节点。
  - `cnt[p]` 表示以节点 `p` 结尾的单词出现次数。
- 查询：路径不存在返回 0，否则返回终点计数。
- 复杂度：插入/查询均为 `O(字符串长度)`。

### 841. 字符串哈希

- 算法：字符串前缀哈希。
- 思路：把字符串看成 P 进制数，用 `h[i]` 表示前 i 个字符的哈希，`p[i]` 表示 `P^i`。
- 子串哈希：
  - `get(l, r) = h[r] - h[l - 1] * p[r - l + 1]`。
- 用途：快速判断两个子串是否相等。
- 复杂度：预处理 `O(n)`，每次查询 `O(1)`。

### KMP 字符串

- 算法：KMP。
- 思路：`ne[i]` 表示模式串前 i 位中最长的相等真前后缀长度；匹配失败时用 `ne[j]` 回退，避免主串指针回退。
- 流程：
  - 先预处理模式串 `next` 数组。
  - 扫描主串，失配就回退 `j`，匹配就递增 `j`。
  - 当 `j == m` 时找到一次匹配，输出起点并继续回退。
- 复杂度：`O(n + m)`。

## 五、DFS、BFS 与树图遍历

### 842. 排列数字

- 算法：DFS 回溯。
- 思路：枚举每个位置填哪个未使用数字。
- 关键：
  - `path[u]` 存当前排列第 `u` 位。
  - `visit[i]` 标记数字 i 是否已用。
  - 递归后要恢复现场。
- 复杂度：`O(n * n!)` 输出所有排列。

### 843. n 皇后问题

- 算法：DFS 回溯 + 剪枝。
- 思路：逐行放皇后，每行选一个合法列。
- 合法性：
  - `col[i]` 判断列是否被占。
  - `dg[u + i]` 判断主对角线。
  - `udg[n - u + i]` 判断副对角线。
- 复杂度：指数级，但剪枝明显减少搜索。

### 844. 走迷宫

- 算法：BFS。
- 思路：从起点开始按层扩展，每个点第一次到达时距离最短。
- 关键：
  - `d[x][y] = -1` 表示未访问。
  - 四方向扩展，满足边界、空地、未访问才入队。
- 复杂度：`O(nm)`。

### 846. 树的重心

- 算法：树形 DFS。
- 思路：删除某个节点后会形成若干连通块，目标是最小化最大连通块大小。
- DFS 返回以 `u` 为根的子树大小：
  - 子节点子树大小是删除 `u` 后的一部分。
  - `n - sum` 是父方向剩余部分。
  - 对每个点取这些部分的最大值，再全局取最小。
- 复杂度：`O(n)`。

### 847. 图中点的层次

- 算法：BFS 求无权图最短路。
- 思路：从 1 号点开始 BFS，`d[j] = d[t] + 1` 记录层数。
- 关键：邻接表存图，`d == -1` 表示未访问。
- 复杂度：`O(n + m)`。

### 848. 有向图的拓扑序列

- 算法：Kahn 拓扑排序。
- 思路：先把所有入度为 0 的点入队；每弹出一个点，就删除它的出边并减少终点入度。
- 关键：
  - 若某个点入度降为 0，入队。
  - 最后若队列输出了 n 个点，则存在拓扑序；否则有环。
- 复杂度：`O(n + m)`。

## 六、最短路

### 849. Dijkstra 求最短路 I：稠密图

- 算法：朴素 Dijkstra。
- 适用：边权非负，稠密图。
- 思路：每轮从未确定最短路的点中选 `dist` 最小的点 `t`，用 `t` 更新其他点。
- 存图：邻接矩阵 `g[a][b]`。
- 复杂度：`O(n^2)`。

### 850. Dijkstra 求最短路 I：稀疏图

- 算法：堆优化 Dijkstra。
- 适用：边权非负，稀疏图。
- 思路：用小根堆维护当前可选的最短距离，每次取堆顶点进行松弛。
- 存图：邻接表 `h, e, ne, w`。
- 复杂度：`O(m log n)`。

### 851. SPFA 求最短路

- 算法：SPFA。
- 适用：可处理负权边，但不能有从源点可达的负环；竞赛中可能被卡。
- 思路：只有某个点距离变小后，才需要用它继续松弛出边，因此把它加入队列。
- 关键：
  - `st[x]` 表示点 x 是否已经在队列里，避免重复入队。
  - 出队后置为 false。
- 平均较快，最坏 `O(nm)`。

### 853. 有边数限制的最短路

- 算法：Bellman-Ford。
- 思路：限制最多经过 k 条边，因此做 k 轮松弛，每轮只允许基于上一轮的结果更新。
- 关键：
  - 每轮开始前备份 `dist` 到 `backup`。
  - 用 `backup[a] + w` 更新 `dist[b]`，避免一轮内连锁更新超过一条边。
- 复杂度：`O(km)`。

### 854. Floyd 求最短路

- 算法：Floyd。
- 适用：多源最短路，点数较少。
- 思路：枚举中转点 `k`，尝试用 `i -> k -> j` 更新 `i -> j`。
- 转移：
  - `dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j])`。
- 复杂度：`O(n^3)`。

## 七、最小生成树

### 858. Prim 算法求最小生成树

- 算法：朴素 Prim。
- 适用：稠密无向图。
- 思路：维护每个点到当前生成树集合的最短边，每轮把集合外距离最短的点加入生成树。
- 关键：
  - `dist[j]` 表示点 j 到当前生成树集合的最小边权。
  - 加入点 `t` 后，用 `g[t][j]` 更新所有点。
  - 若非首个点的 `dist[t]` 为 INF，说明图不连通。
- 复杂度：`O(n^2)`。

### 859. Kruskal 算法求最小生成树

- 算法：Kruskal + 并查集。
- 适用：稀疏无向图。
- 思路：把所有边按边权从小到大排序，依次尝试加入；若一条边连接两个不同连通块，就加入生成树并合并集合。
- 关键：
  - 用并查集判断是否成环。
  - 最后若加入边数小于 `n - 1`，说明图不连通。
- 复杂度：排序 `O(m log m)`。

## 八、二分图

### 860. 染色法判定二分图

- 算法：DFS 染色。
- 思路：把图的相邻点染成不同颜色，如果出现一条边两端颜色相同，就不是二分图。
- 关键：
  - 颜色 0 表示未染色，1/2 表示两种颜色。
  - 对每个未染色连通块都要启动一次 DFS。
- 复杂度：`O(n + m)`。

### 861. 二分图的最大匹配

- 状态：当前文件为空。
- 常用算法：匈牙利算法。
- 思路：依次枚举左部点，尝试给它找一个右部匹配点；若右部点已被别人匹配，则递归尝试让原匹配点换一个对象。
- 关键：
  - `match[j]` 表示右部点 j 当前匹配的左部点。
  - 每轮为当前左部点新建 `st[]`，避免同一轮重复尝试同一个右部点。
- 复杂度：通常写法 `O(nm)`。

## 九、STL 速记

`STL.txt` 主要记录了竞赛常用容器接口，可按用途记：

- `vector`：动态数组，支持 `push_back`、随机访问、遍历。
- `string`：字符串，支持 `substr`、`c_str`、长度查询。
- `queue`：普通队列，常用于 BFS。
- `priority_queue`：堆，默认大根堆；小根堆写法是 `priority_queue<int, vector<int>, greater<int>>`。
- `stack`：栈，常用于单调栈、表达式、DFS 辅助。
- `deque`：双端队列，常用于单调队列。
- `set/map`：有序集合/映射，支持 `lower_bound`、`upper_bound`，单次操作 `O(log n)`。
- `unordered_set/unordered_map`：哈希集合/映射，均摊 `O(1)`，不支持有序查询。
- `bitset`：压位布尔数组，支持位运算、计数和翻转。

## 十、整体复习路线

1. 先背熟基础模板：快选、离散化、区间合并、单调栈/队列。
2. 再掌握数据结构模板：链表、Trie、并查集、哈希、堆。
3. 搜索部分重点区分：
   - DFS：枚举方案、回溯、剪枝。
   - BFS：最短步数、层次遍历、无权图最短路。
4. 图论按问题类型记：
   - 拓扑序：有向无环依赖。
   - Dijkstra：非负权单源最短路。
   - Bellman-Ford：限制边数或负权边。
   - Floyd：多源最短路。
   - Prim/Kruskal：最小生成树。
   - 染色/匈牙利：二分图。
5. 最后补齐空文件：`827. 双链表.cpp`、`838. 堆排序.cpp`、`861. 二分图的最大匹配.cpp`。

